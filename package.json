{
  "name": "IconExplorer",
  "version": "1.0.0",
  "description": "UI5 Icon Explorer PWA",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "node server.js --use_strict", "build": "grunt build"
  },
  "author": "SAP SE",
  "license": "Apache-2.0",
  "dependencies": {
    "express": "^4.15.3"
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-clean": "^0.6.0",
    "grunt-contrib-concat": "^0.5.1",
    "grunt-curl": "^2.2.1",
    "grunt-openui5": "^0.8.0"
  }
}
